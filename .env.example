# Lấy từ Supabase Dashboard > Settings > API
# URL dạng: https://your-project-id.supabase.co
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Anon/Public Key - Dùng cho client-side
# Key này an toàn để expose ra client
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key - CHỈ dùng cho server-side (Edge Functions)
# KHÔNG BAO GIỜ expose key này ra client!
# Chỉ dùng trong Edge Functions hoặc backend API
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==============================================
# TWILIO CONFIGURATION (Optional - cho SMS)
# ==============================================
# Lấy từ Twilio Console: https://console.twilio.com/
# Account SID
VITE_TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Auth Token
VITE_TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Số điện thoại Twilio (định dạng: +1234567890)
VITE_TWILIO_PHONE_NUMBER=+1234567890

# ==============================================
# GOOGLE MAPS API (Optional)
# ==============================================
# Nếu sử dụng Google Maps thay vì Leaflet
# Lấy từ: https://console.cloud.google.com/
VITE_GOOGLE_MAPS_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ==============================================
# APPLICATION SETTINGS
# ==============================================
# Tên ứng dụng hiển thị
VITE_APP_NAME="Hệ Thống Quản Lý Độ Mặn"

# URL của ứng dụng
# Development: http://localhost:5173
# Production: https://your-domain.com
VITE_APP_URL=http://localhost:5173

# Môi trường: development, staging, production
VITE_ENV=development

# ==============================================
# PROPHET API (nếu có backend riêng cho AI)
# ==============================================
# URL của API backend xử lý dự báo Prophet
# Ví dụ: Flask/FastAPI server chạy Prophet model
VITE_PROPHET_API_URL=http://localhost:8000/api

# API Key cho Prophet service (nếu cần)
VITE_PROPHET_API_KEY=your-prophet-api-key

# ==============================================
# FEATURE FLAGS (Optional)
# ==============================================
# Bật/tắt tính năng SMS
VITE_ENABLE_SMS=true

# Bật/tắt Analytics tracking
VITE_ENABLE_ANALYTICS=true

# Bật/tắt xác thực 2 yếu tố (2FA)
VITE_ENABLE_2FA=true

# Bật/tắt chế độ debug
VITE_DEBUG_MODE=false

# Bật/tắt tính năng dự báo AI
VITE_ENABLE_FORECAST=true

# ==============================================
# SMTP CONFIGURATION (cho Edge Functions)
# ==============================================
# Cấu hình email server (không cần prefix VITE_ vì chỉ dùng server-side)
# Dùng cho Edge Functions gửi email

# SMTP Host (ví dụ: smtp.gmail.com)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 cho TLS, 465 cho SSL)
SMTP_PORT=587

# SMTP Username (thường là email)
SMTP_USER=your-email@gmail.com

# SMTP Password (App Password nếu dùng Gmail)
SMTP_PASS=your-app-password

# Email người gửi
SMTP_FROM=noreply@your-domain.com

# ==============================================
# ANALYTICS & MONITORING (Optional)
# ==============================================
# Google Analytics Measurement ID
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN cho error tracking
VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ==============================================
# RATE LIMITING (Optional)
# ==============================================
# Giới hạn số request cho API
# Số request tối đa trong 1 phút
VITE_RATE_LIMIT_PER_MINUTE=60

# ==============================================
# STORAGE & UPLOAD LIMITS
# ==============================================
# Kích thước tối đa file upload (MB)
VITE_MAX_FILE_SIZE_MB=5

# Số lượng ảnh tối đa trong 1 bài viết
VITE_MAX_IMAGES_PER_POST=5

# Kích thước video tối đa (MB)
VITE_MAX_VIDEO_SIZE_MB=50

# ==============================================
# SECURITY
# ==============================================
# JWT Secret (cho Edge Functions nếu cần custom JWT)
JWT_SECRET=your-very-long-and-secure-secret-key-here

# Session timeout (giây)
VITE_SESSION_TIMEOUT=3600

# ==============================================
# DEVELOPMENT ONLY
# ==============================================
# Port cho dev server (mặc định: 5173)
# VITE_PORT=5173

# Bật source maps trong production (không khuyến nghị)
# VITE_GENERATE_SOURCEMAP=false

# ==============================================
# NOTES & TIPS
# ==============================================
#
# 1. PREFIX "VITE_":
#    - Variables với prefix VITE_ sẽ được expose ra client-side
#    - Variables KHÔNG có prefix chỉ dùng server-side (Edge Functions)
#
# 2. SECURITY:
#    - KHÔNG commit file .env lên Git
#    - Thêm .env vào .gitignore
#    - Service Role Key TUYỆT ĐỐI không expose ra client
#    - Sử dụng .env.example làm template
#
# 3. ENVIRONMENTS:
#    - Development: .env.development
#    - Production: .env.production
#    - Staging: .env.staging
#    - Local: .env.local (override tất cả, không commit)
#
# 4. VALIDATION:
#    - Kiểm tra tất cả variables bắt buộc trước khi chạy
#    - Log warning nếu thiếu variables quan trọng
#
# 5. EXAMPLES:
#    - Không để giá trị thật trong file .env.example
#    - Chỉ để placeholder và hướng dẫn
#
# ==============================================

# ==============================================
# VARIABLES BẮT BUỘC
# ==============================================
# Các variables sau BẮT BUỘC phải có:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - VITE_APP_URL
#
# Các variables optional (tùy tính năng sử dụng):
# - VITE_TWILIO_* (nếu dùng SMS)
# - VITE_GOOGLE_MAPS_API_KEY (nếu dùng Google Maps)
# - VITE_PROPHET_API_URL (nếu có backend AI riêng)
# - SMTP_* (nếu Edge Functions gửi email)
# - VITE_GA_MEASUREMENT_ID (nếu dùng Google Analytics)
# - VITE_SENTRY_DSN (nếu dùng Sentry)
#
# ==============================================
